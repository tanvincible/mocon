{
  "name": "mocon-workspace",
  "private": true,
  "description": "Reference TypeScript stack for mocon, an observability record format for code-mode MCP hosts.",
  "license": "MIT OR Apache-2.0",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "workspaces": [
    "packages/*",
    "examples/*"
  ],
  "scripts": {
    "build": "tsc -b packages/core packages/otel packages/adapter-mcp packages/cli examples/node-vm-codemode",
    "test": "npm run build && npm run test --workspaces --if-present",
    "bench": "npm run build && node bench/hot-path.mjs",
    "conformance": "python3 spec/conformance/check.py all"
  },
  "devDependencies": {
    "@modelcontextprotocol/sdk": "^1.30.0",
    "@types/node": "^20.19.0",
    "ajv": "^8.17.0",
    "tsx": "^4.20.0",
    "typescript": "^5.9.0"
  }
}
