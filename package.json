{
  "name": "mocon-workspace",
  "private": true,
  "description": "The mocon specification for code-mode execution, and its reference emitter on the OpenTelemetry API.",
  "license": "MIT OR Apache-2.0",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "tsc -b packages/typescript",
    "test": "npm run build && npm run test --workspaces --if-present",
    "bench": "npm run build && node bench/trace.mjs",
    "docs": "mdbook build docs && cp docs/src/llms.txt docs/book/llms.txt",
    "docs:serve": "mdbook serve docs --open"
  },
  "devDependencies": {
    "@opentelemetry/api": "^1.9.1",
    "@opentelemetry/exporter-trace-otlp-http": "^0.57.2",
    "@opentelemetry/resources": "^1.30.1",
    "@opentelemetry/sdk-trace-base": "^1.30.1",
    "@types/node": "^20.19.0",
    "tsx": "^4.20.0",
    "typescript": "^5.9.0"
  }
}
