{
  "name": "@mocon/example-node-vm-codemode",
  "version": "0.1.0",
  "private": true,
  "description": "A code-mode MCP server over node:vm, instrumented with mocon. Demonstration only.",
  "license": "MIT OR Apache-2.0",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "tsc -b",
    "example": "npm run build && node dist/drive.js",
    "test": "tsc -b && node --import tsx --test test/*.test.ts",
    "typecheck": "tsc -p test/tsconfig.json"
  },
  "dependencies": {
    "@mocon/adapter-mcp": "^0.1.0",
    "@mocon/cli": "^0.1.0",
    "@mocon/core": "^0.1.0",
    "@modelcontextprotocol/sdk": "^1.30.0",
    "zod": "^4.0.0"
  },
  "devDependencies": {
    "fast-check": "^3.23.2"
  }
}
