{
  "name": "@mocon/cli",
  "version": "0.1.0",
  "description": "Command line tools for mocon streams: validate, view, ui, otlp.",
  "license": "MIT OR Apache-2.0",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "bin": {
    "mocon": "./dist/bin.js"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -b",
    "test": "node --import tsx --test test/*.test.ts",
    "typecheck": "tsc -p test/tsconfig.json"
  },
  "dependencies": {
    "@mocon/core": "^0.1.0",
    "@mocon/otel": "^0.1.0"
  },
  "devDependencies": {
    "fast-check": "^3.23.2"
  }
}
