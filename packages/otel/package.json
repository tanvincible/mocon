{
  "name": "@mocon/otel",
  "version": "0.1.0",
  "description": "OTLP/JSON sink and pure line-to-span mapping for mocon. No OpenTelemetry dependency.",
  "license": "MIT OR Apache-2.0",
  "type": "module",
  "sideEffects": false,
  "engines": {
    "node": ">=20"
  },
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -b",
    "test": "node --import tsx --test test/*.test.ts",
    "typecheck": "tsc -p test/tsconfig.json"
  },
  "dependencies": {
    "@mocon/core": "^0.1.0"
  },
  "devDependencies": {
    "fast-check": "^3.23.2"
  }
}
