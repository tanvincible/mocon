{
  "resourceSpans": [],
  "_note": "unresolved.jsonl carries a host declaration (host: meigma/codemode) and exactly one execution line, id e7317286b59fedf2aa9d2dd6f2c11911, and that execution line never carries \"end\" anywhere in the stream. core.md section 4 calls a line without \"end\" a start notice, and a record that only ever has a start notice \"unresolved\" (rule 5): a consumer MUST show it as running or unknown, never with any disposition. otel-mapping.md section 3 says a sink drops every line without \"end\" because an OTLP trace exporter has no way to represent a running record (core.md section 4 rule 7), and section 3's \"Unresolved executions\" paragraph is explicit that a sink MUST NOT synthesize an abandoned span after a timeout of its own and MUST NOT invent a disposition for anything. The host record itself never produces a span either (otel-mapping.md section 12: the host line is one of the two core inputs with no attribute or span home). So a conforming stateless sink reading this exact stream emits zero spans, ever, for as long as the stream contains no complete record for this execution. This file is that: a syntactically valid ExportTraceServiceRequest with an empty resourceSpans array. It is not an error case and not a placeholder; it is the correct, final OTLP output for this stream. The \"_note\" key itself is not part of OTLP and is not compared by conformance checks; see otlp/README.md."
}
